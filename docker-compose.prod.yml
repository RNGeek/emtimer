version: '3'
services:
  emtimer:
    build: .
    command: build yarn run start
    environment:
      NODE_VERSION: 6.11.3
      NPM_VERSION: 5.4.1
      YARN_VERSION: 0.28.4
      PORT: 8080
    ports:
      - '8080:8080'
    volumes:
      - .:/opt/repo
